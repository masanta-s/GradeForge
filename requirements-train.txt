# OPTIONAL local LLM fine-tuning environment (.venv-train) — NOT needed on an 8 GB GPU.
# On this laptop LLM fine-tuning is exported to Colab/Kaggle instead.
# Pinned to Unsloth 2026.9.4's upper bounds (torch<2.13, transformers<=5.5.0, trl<=0.24.0, datasets<4.4).
# Verified to resolve on Python 3.14 with wheels only (2026-09-10).
#
#   py -3.14 -m venv .venv-train
#   . .\env.ps1 ; .venv-train\Scripts\python -m pip install -r requirements-train.txt

--extra-index-url https://download.pytorch.org/whl/cu132

torch==2.12.1+cu132
torchvision==0.27.1+cu132
unsloth==2026.9.4
unsloth_zoo==2026.9.3
transformers==5.5.0
trl==0.24.0
datasets==4.3.0
peft==0.20.0
bitsandbytes==0.50.2
torchao==0.18.0
triton-windows==3.8.0.post28
xformers==0.0.35
