# Environments & caches (all kept on this drive — see env.ps1)
.venv/
.venv-train/
.cache/
/tools/
__pycache__/
*.pyc
.pytest_cache/

# Model weights (downloaded by setup_env.py) — root only; src/models/ is source code
/models/

# Student data — never commit
/data/

# Frontend
frontend/node_modules/
frontend/dist/

# IDE
.idea/
.junie/

# Local tooling config
.claude/

# Secrets: the example is committed, the real file is not
.env
